# Architecture Notes

This directory exists to provide a Motherlode-compatible lowercase architecture path.

The primary system design document remains [docs/ARCHITECTURE.md](../ARCHITECTURE.md), which covers:

- the Rust core policy engine
- the browser extension execution model
- future daemon, proxy, and GUI integration paths
- testing and security considerations

Future architecture decision records can be added here as separate markdown files.
